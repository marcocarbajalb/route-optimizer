# Google Maps Distance Matrix API key (handled as a Secret during deploy)
GOOGLE_MAPS_API_KEY=

# Comma-separated list of allowed public IPs.
# If empty, ALL requests are rejected (fail-closed).
ALLOWED_IPS=