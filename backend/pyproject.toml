[project]
name = "backend"
version = "0.1.0"
description = "Route optimization with genetic algorithm using Firebase Cloud Functions and Google Maps API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "firebase-admin>=7.4.0",
    "firebase-functions>=0.4.2",
    "pydantic>=2.13.4",
    "requests>=2.34.2",
]